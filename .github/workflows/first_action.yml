name: First Workflow
on: workflow_dispatch
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
            echo "Hello World!"
            echo "What do you think about this workdlow"
      - name: Print goodbye
        run: echo "Never mind, goodbye!"
        
